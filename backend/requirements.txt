# Meetolog Backend Dependencies (v3)

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Configuration
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Speech-to-Text (Local Whisper model)
# Install CPU-only PyTorch first (much smaller than default with CUDA)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchaudio>=2.0.0
openai-whisper>=20231117

# LLM Providers
google-generativeai>=0.3.0
openai>=1.12.0

# PDF Generation
reportlab>=4.0.0

# File handling
python-multipart>=0.0.6

# AWS S3 (async)
aioboto3>=13.0.0

# PostgreSQL (async ORM)
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.30.0
alembic>=1.13.0
python-dotenv>=1.0.0
aiosqlite>=0.20.0  # For testing with in-memory SQLite

# Token counting for hierarchical summarization
tiktoken>=0.7.0

# Vector math for RAG retrieval (transitive via torch, pinned explicitly)
numpy>=1.24.0

# Persistent vector storage (required only when RAG_STORAGE_BACKEND=pgvector)
pgvector>=0.3.0

# Retry / resilience
tenacity>=9.0.0

# JSON repair for malformed LLM responses
json-repair>=0.30.0

# Structured logging
structlog>=24.1.0

# Speaker diarization (requires HuggingFace token + model agreement)
pyannote.audio>=3.1.0

# Whisper dependencies (torch is auto-installed by openai-whisper)
# ffmpeg-python  # Optional: uncomment if you need programmatic ffmpeg access
